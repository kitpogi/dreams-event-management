# Payment Flow - Visual Diagram

## 🔄 Simple Payment Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    PAYMENT FLOW DIAGRAM                         │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│    USER      │
└──────┬───────┘
       │
       │ 1. Creates Booking
       ▼
┌─────────────────────┐
│  Booking Created     │
│  Status: pending     │
│  Payment: unpaid     │
└──────┬──────────────┘
       │
       │ 2. Clicks "Pay Now"
       ▼
┌─────────────────────┐
│  PaymentForm Opens   │
│  User selects method │
└──────┬──────────────┘
       │
       │ 3. "Continue to Payment"
       ▼
┌─────────────────────────────────────────────────────────┐
│  FRONTEND: createPaymentIntent()                        │
│  POST /api/payments/create-intent                      │
│  { booking_id, amount, payment_methods }                │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  BACKEND: PaymentController::createPaymentIntent()      │
│  - Validates request                                     │
│  - Checks user authorization                            │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  BACKEND: PaymentService::createPaymentIntent()        │
│  - Converts amount to centavos (×100)                   │
│  - Calls PayMongo API                                   │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  PAYMONGO API                                           │
│  POST /payment_intents                                  │
│  Returns: payment_intent_id, client_key                 │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  BACKEND: Creates Payment Record                        │
│  payments table:                                        │
│  - payment_intent_id                                    │
│  - amount                                               │
│  - status: pending                                      │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  FRONTEND: Receives Response                            │
│  { payment_intent_id, client_key }                      │
│  Initializes PayMongo SDK                               │
└──────┬──────────────────────────────────────────────────┘
       │
       │
       ├──────────────────┬──────────────────┐
       │                  │                  │
       ▼                  ▼                  ▼
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│   CARD      │  │  E-WALLET   │  │  BANK TRANS │
│  PAYMENT    │  │  PAYMENT    │  │  PAYMENT    │
└──────┬──────┘  └──────┬──────┘  └──────┬──────┘
       │                │                 │
       │                │                 │
       │ User enters    │ User redirected │ User redirected
       │ card details   │ to PayMongo     │ to PayMongo
       │                │ checkout        │ checkout
       │                │                 │
       └────────┬───────┴────────┬────────┘
                │                │
                │ PayMongo returns paymentMethodId
                ▼
┌─────────────────────────────────────────────────────────┐
│  FRONTEND: attachPaymentMethod()                       │
│  POST /api/payments/attach-method                      │
│  { payment_intent_id, payment_method_id }              │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  BACKEND: PaymentService::attachPaymentMethod()       │
│  - Calls PayMongo to attach method                     │
│  - PayMongo processes payment                          │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  BACKEND: Updates Payment Record                       │
│  status: processing or paid                            │
└──────┬──────────────────────────────────────────────────┘
       │
       │ (Asynchronous - happens in background)
       ▼
┌─────────────────────────────────────────────────────────┐
│  PAYMONGO: Processes Payment                            │
│  (Takes a few seconds to minutes)                      │
└──────┬──────────────────────────────────────────────────┘
       │
       │ Webhook sent
       ▼
┌─────────────────────────────────────────────────────────┐
│  BACKEND: Webhook Handler                              │
│  POST /api/payments/webhook                            │
│  - Verifies signature                                  │
│  - Processes event                                     │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  BACKEND: PaymentService::processWebhook()             │
│  Event: payment_intent.succeeded                       │
│  - Updates payment status → paid                        │
│  - Sets paid_at timestamp                              │
│  - Updates booking payment status                      │
└──────┬──────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│  USER: Views Payment Status                            │
│  PaymentConfirmation page                              │
│  Shows: paid, amount, booking details                 │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Payment States Flow

```
┌──────────┐
│ pending  │ ← Payment intent created
└────┬─────┘
     │
     │ Payment method attached
     ▼
┌──────────────┐
│ processing   │ ← Payment being processed
└────┬─────────┘
     │
     ├──────────────┬──────────────┐
     │              │              │
     ▼              ▼              ▼
┌─────────┐  ┌──────────┐  ┌──────────┐
│  paid   │  │  failed  │  │cancelled │
└─────────┘  └──────────┘  └──────────┘
   ✅            ❌            ⚠️
```

---

## 🔄 Booking Payment Status Flow

```
┌─────────┐
│ unpaid  │ ← No payments made
└────┬────┘
     │
     │ First payment made
     ▼
┌──────────┐
│ partial  │ ← Some payments, not full amount
└────┬─────┘
     │
     │ Total paid ≥ total amount
     ▼
┌─────────┐
│  paid   │ ← Fully paid
└─────────┘
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────┐
│  WEBHOOK SECURITY                                       │
└─────────────────────────────────────────────────────────┘

PayMongo → Webhook Request
    │
    ├─> Paymongo-Signature Header
    │
    ▼
Backend: verifyWebhookSignature()
    │
    ├─> Computes HMAC SHA256
    │   (payload + webhook_secret)
    │
    ├─> Compares with signature
    │
    └─> ✅ Valid → Process webhook
        ❌ Invalid → Reject (401)
```

---

## 💳 Payment Method Flows

### Card Payment:
```
User → PayMongo Form → Card Details → paymentMethodId → Attach → Process
```

### E-Wallet Payment:
```
User → Redirect → PayMongo Checkout → E-Wallet App → paymentMethodId → Attach → Process
```

### Bank Transfer:
```
User → Redirect → PayMongo Checkout → Bank Details → paymentMethodId → Attach → Process
```

---

## 📱 User Journey (Simplified)

```
1. Create Booking
   └─> Booking: pending, unpaid

2. Click "Pay Now"
   └─> PaymentForm opens

3. Select Payment Method
   └─> Card / GCash / Maya / QR Ph / Bank

4. Continue to Payment
   └─> Payment Intent Created
       └─> Payment: pending

5. Complete Payment
   ├─> Card: Enter details in form
   └─> E-Wallet: Redirect to PayMongo

6. Payment Processing
   └─> Payment: processing

7. Webhook Confirmation
   └─> Payment: paid
       └─> Booking: paid (if full amount)

8. View Confirmation
   └─> PaymentConfirmation page
```

---

## 🎯 Key Points

1. **Payment Intent** = Request to collect payment (created first)
2. **Payment Method** = How user wants to pay (card, GCash, etc.)
3. **Attach Method** = Link payment method to intent (triggers processing)
4. **Webhook** = PayMongo confirms payment (updates status)
5. **Status Check** = User can check payment status anytime

---

## 🔄 Complete Flow in One View

```
USER ACTION          FRONTEND              BACKEND              PAYMONGO
─────────────────────────────────────────────────────────────────────────
1. Create Booking    BookingForm           BookingController    -
                     └─> Creates booking   └─> Saves booking
                     
2. Click Pay Now     PaymentForm opens     -                    -
                     
3. Select Method     User selects          -                    -
                     payment method
                     
4. Continue Payment  createPaymentIntent() PaymentController    -
                     └─> POST /create-     └─> Validates        -
                         intent            └─> PaymentService    -
                                           └─> POST /payment_    └─> Creates
                                               intents              intent
                                                                  └─> Returns
                                                                      intent_id
                                                                      
5. Receive Intent    Receives intent_id    Creates payment      -
                     & client_key          record (pending)
                     
6. Initialize SDK    Loads PayMongo SDK    -                    -
                     Initializes form
                     
7. User Pays         Card: Form input      -                    -
                     E-Wallet: Redirect    -                    └─> Processes
                                                                      payment
                     
8. Attach Method     attachPaymentMethod() PaymentController    -
                     └─> POST /attach-     └─> PaymentService   └─> Attaches
                         method            └─> POST /attach         method
                                                                  └─> Processes
                                                                      
9. Webhook           -                     Webhook handler      └─> Sends
                     └─> Updates UI        └─> Verifies sig        webhook
                         (polling)         └─> Updates status
                                           └─> Updates booking
                                               
10. View Status      PaymentConfirmation   PaymentController    -
                     └─> GET /status      └─> Gets status      └─> Returns
                                                                    status
```

---

This diagram shows the complete flow from user action to payment confirmation!
